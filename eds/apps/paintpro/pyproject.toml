[project]
name = "paint-pro-estimator"
version = "0.1.0"
description = "PaintPro Estimator + Invoice System (base version)"
readme = "paint_pro_estimator/README.md"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project.scripts]
paint-pro-estimator = "paint_pro_estimator.cli:main"
